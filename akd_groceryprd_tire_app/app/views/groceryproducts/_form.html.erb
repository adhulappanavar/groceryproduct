<%= form_for(@groceryproduct) do |f| %>
  <% if @groceryproduct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groceryproduct.errors.count, "error") %> prohibited this groceryproduct from being saved:</h2>

      <ul>
      <% @groceryproduct.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :published_on %><br>
    <%= f.date_select :published_on %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
